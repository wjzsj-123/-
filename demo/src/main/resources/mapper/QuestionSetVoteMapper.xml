<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.demos.web.mapper.QuestionSetVoteMapper">
    <select id="selectBySetIdAndUserId" resultType="com.example.demo.demos.web.pojo.QuestionSetVote">
        SELECT id, question_set_id, user_id, vote_type, create_time, update_time
        FROM question_set_vote
        WHERE question_set_id = #{questionSetId}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.demo.demos.web.pojo.QuestionSetVote"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO question_set_vote (
            question_set_id, user_id, vote_type, create_time, update_time
        ) VALUES (
            #{questionSetId}, #{userId}, #{voteType}, NOW(), NOW()
        )
    </insert>

    <update id="updateVoteType">
        UPDATE question_set_vote
        SET vote_type = #{voteType},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM question_set_vote WHERE id = #{id}
    </delete>

    <delete id="deleteByQuestionSetId">
        DELETE FROM question_set_vote WHERE question_set_id = #{questionSetId}
    </delete>
</mapper>
